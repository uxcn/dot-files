# print dir up
pup() {

  local p
  local n

  n=${1:-1}
  p=${PWD}

  if (( n < 0 )); then

    let n="-n"
    pd $n

    exit 0

  fi
  
  while (( n-- > 0 )); do

    p=$(dirname $p)

  done

  echo "$p"

}

# vim:ft=zsh:et:ts=2:sts=2:sw=2:tw=80
